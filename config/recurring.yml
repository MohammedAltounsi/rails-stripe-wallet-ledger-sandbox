# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

# No recurring tasks. Solid Queue is not run in this deployment (Active Job uses
# the :async adapter), so a production task here would make the Solid Queue engine
# query tables that aren't provisioned, logging a PG::UndefinedTable on boot.
